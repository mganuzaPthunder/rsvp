# Durable storage for RSVP replies (Vercel Marketplace → Upstash for Redis).
# Vercel injects these automatically once the store is connected to the project.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Any long random string. Guards /admin?key=...
ADMIN_KEY=
